#include "main.h"

/**
 * mul - function to multiply two numbers
 * @a: first number to multiply
 * @b: second number to multiply
 *
 * Return: 0 (success)
 */

int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
