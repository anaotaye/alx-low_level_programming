#include <stdlib.h>
#include <unistd.h>
#include <ctype.h>
#include "main.h"

/**
 * _abs - function to return the absolute value of a number
 *
 * Return: 0 (success)
 */

int _abs(int)
{
	return (abs(int));
	return (0);
}
