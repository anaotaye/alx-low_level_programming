#include <stdlib.h>
#include <unistd.h>
#include <ctype.h>
#include "main.h"

/**
 * _abs - function to return the absolute value of a number
 *
 *@n: integer to be checked
 *
 * Return: 0 (success)
 */

int _abs(int n)
{
	return (abs(n));
	return (0);
}
